# Evaluation settings (shared across datasets)
c_thought: 1
epochs_per_stage: 5
max_latent_stage: 6
pad_latent_to_max: true
uniform_prob: 0.0
reset_optimizer: false
batch_size_training: 32
gradient_accumulation_steps: 1
num_epochs: 50
lr: 0.0001
weight_decay: 0.01
eval_every_n_epochs: 1          # Evaluate every N epochs (1 = every epoch)

